export const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
export const passwordRegex = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[^\da-zA-Z]).{8,}$/;
export const theme = {
  colors: {
    primary: '#FFC107',
    scondary: '#FF5929',
    tertiary: '#2E5EB9',
    darkgrey: '#8a8a8a',
    grey: '#c9c9c9',
    light: '#FFFFFF',
    dark: '#000000',
  }
}